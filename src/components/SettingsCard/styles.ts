import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
    padding: 25,
    paddingTop: 0,
    marginTop: 12
  },
  button: {
    marginTop: 12,
    padding: 15,
    width: '100%',
    borderWidth: 1,
    borderRadius: 12
  },
  title: {
    fontWeight: '700'
  }
});